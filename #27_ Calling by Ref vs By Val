أكيد سفيان! خليني نشرحلك الفرق بين **التمرير بالقيمة (Call by Value)** و **التمرير بالإشارة (Call by Reference)** في C++ بطريقة مبسطة وبأمثلة.

---

## 🔹 Call by Value (التمرير بالقيمة)

في هذا النوع، عندما تمرر متغيرًا إلى دالة، يتم **إنشاء نسخة** من هذا المتغير. يعني التغييرات داخل الدالة **لا تؤثر** على المتغير الأصلي.

### ✅ مثال:

```cpp
#include <iostream>
using namespace std;

void changeValue(int x) {
    x = 100;
}

int main() {
    int a = 10;
    changeValue(a);
    cout << a << endl;  // الناتج: 10
    return 0;
}
```

🔸 **الشرح**: المتغير `x` هو نسخة من `a`، وتم تغييره داخل الدالة، لكن `a` بقي كما هو.

---

## 🔹 Call by Reference (التمرير بالإشارة)

هنا، تمرر **عنوان المتغير** للدالة، يعني أي تغيير يتم داخل الدالة **يؤثر مباشرة** على المتغير الأصلي.

### ✅ مثال:

```cpp
#include <iostream>
using namespace std;

void changeValue(int &x) {
    x = 100;
}

int main() {
    int a = 10;
    changeValue(a);
    cout << a << endl;  // الناتج: 100
    return 0;
}
```

🔸 **الشرح**: هنا `x` هو **مرجع** لـ `a`، لذلك أي تغيير عليه يؤثر على `a` مباشرة.

---

## ✳️ مقارنة سريعة:

| الجانب                 | Call by Value            | Call by Reference       |
|------------------------|---------------------------|--------------------------|
| نوع التمرير            | نسخة من المتغير           | عنوان المتغير الأصلي     |
| تأثير على المتغير الأصلي | لا                        | نعم                      |
| الاستخدام               | آمن أكثر، لا يغير القيم   | أكثر كفاءة وأسرع أحيانًا |

---


