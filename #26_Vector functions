في C++، مكتبة vector من STL (Standard Template Library) كتقدم مجموعة من الدوال (Functions) المهمة باش نتعاملو مع الـ vectors.
ها أهم الدوال اللي كتستعمل بزاف، وغادي نشرحهم ليك بشكل مبسط باللهجة المغربية:


---

1. size()

كتعطينا عدد العناصر اللي كاينين فـ vector.

v.size(); // مثلا: 3


---

2. capacity()

كتعطينا شحال من عنصر يقدر الـ vector يخزن بلا ما يزيد فالحجم.


---

3. push_back(value)

كتضيف عنصر فـ الآخر ديال الـ vector.

v.push_back(10);


---

4. pop_back()

كتحيد آخر عنصر من الـ vector.

v.pop_back();


---

5. front()

كتعطينا أول عنصر.

int x = v.front();


---

6. back()

كتعطينا آخر عنصر.

int y = v.back();


---

7. clear()

كتفرّغ الـ vector كامل.

v.clear();


---

8. empty()

كترد true إلا كان الـ vector خاوي.

if(v.empty()) {
  cout << "vector خاوي";
}


---

9. at(index)

كتعطينا القيمة اللي فموقع معيّن (بحال المصفوفة).

v.at(0); // أول عنصر


---

10. insert(position, value)

كتدخل عنصر فموقع معيّن.

v.insert(v.begin() + 1, 50); // تدير 50 فالموقع الثاني


---

11. erase(position)

كتحيد عنصر من موقع معيّن.

v.erase(v.begin() + 2); // تحيد العنصر الثالث


---

12. resize(n)

كتبدل حجم الـ vector. إلا زدت الحجم، كيعمر القيم بـ 0.

v.resize(5);


---

13. shrink_to_fit()

كتنقص السعة وتخليها مساوية للحجم الحقيقي.


---
